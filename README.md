# ScriptableObject-Auto-Creator
 A Unity package that automatically creates ScriptableObject assets from an attribute and ensures they exist at a specified path without requiring manual creation through the Create Asset menu.
